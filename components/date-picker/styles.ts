import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    today: {
        borderColor: '#007AFF',
        borderWidth: 1,
    },
    selected: {
        backgroundColor: '#007AFF',
    },
    selected_label: {
        color: '#fff',
        fontWeight: '600',
    },
});